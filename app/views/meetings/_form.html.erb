<%= form_with(model: meeting) do |form| %>
  <% if meeting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
        <% meeting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.label :partner_id, style: "display: block" %>
    <%= form.text_field :partner_id %>
  </div>

  <div>
    <%= form.label :pioneer_id, style: "display: block" %>
    <%= form.text_field :pioneer_id %>
  </div>

  <div>
    <%= form.label :part_time_id, style: "display: block" %>
    <%= form.text_field :part_time_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
